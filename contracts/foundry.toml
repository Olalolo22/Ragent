[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
optimizer = true
optimizer_runs = 200
evm_version = "paris"  # Compatible with most L2s / Arc

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
