# Ragent Coordinator
OPENAI_API_KEY=          # optional - enables real LLM for dynamic policies and bids (highly recommended for demo)

# Arc Testnet + Full Escrow + ERC-8004 (HIGH PRIORITY for submission)
USE_TESTNET=true
ARC_RPC=https://rpc.testnet.arc.network
PRIVATE_KEY=0x...               # must have test USDC (gas + token)
USDC_ADDRESS=0x...              # real USDC address on Arc testnet

# Local anvil (default)
# (no extra vars needed)

# For demo agents
DEFAULT_PROVIDER_ADDRESSES=0xA1,0xB2,0xC3
